'use strict';

const expect = require('chai').expect;
const ErrorRegister = require('../index');

describe('JS :: Babel-less tests', function() {
    it('t1', function() {
        expect(0).to.equal(1);
    });
});
